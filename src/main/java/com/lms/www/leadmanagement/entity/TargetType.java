package com.lms.www.leadmanagement.entity;

public enum TargetType {
    ASSIGNED,     // Manager -> User (Defining their quota)
    DISTRIBUTED   // User -> self/subordinates (Defining how they use their quota)
}
